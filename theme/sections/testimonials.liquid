<section class="testimonials py-12">
  <div class="container">
    {%- if section.settings.title != blank -%}
      <div class="section-header text-center mb-4">
        <h2 class="section-title">{{ section.settings.title }}</h2>
      </div>
    {%- endif -%}

    <div class="testimonials__grid grid grid-cols-1 md:grid-cols-3">
      {%- for block in section.blocks -%}
        <div class="testimonial card" {{ block.shopify_attributes }}>
          <div class="testimonial__rating">
            {%- for i in (1..5) -%}
              <span class="testimonial__star">
                {%- render 'icon-star' -%}
              </span>
            {%- endfor -%}
          </div>

          <blockquote class="testimonial__text">
            {{ block.settings.text }}
          </blockquote>

          <div class="testimonial__author">
            <p class="testimonial__name">{{ block.settings.author }}</p>
            {%- if block.settings.product != blank -%}
              <p class="testimonial__product">{{ block.settings.product }}</p>
            {%- endif -%}
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

<style>
  .testimonials__grid {
    gap: var(--spacing-6);
  }

  .testimonial {
    padding: var(--spacing-6);
  }

  .testimonial__rating {
    display: flex;
    gap: var(--spacing-1);
    margin-bottom: var(--spacing-4);
    color: var(--color-cta-orange);
  }

  .testimonial__star {
    width: 20px;
    height: 20px;
  }

  .testimonial__text {
    font-size: var(--font-size-base);
    color: var(--color-text-primary);
    line-height: var(--line-height-relaxed);
    margin: 0 0 var(--spacing-4) 0;
  }

  .testimonial__name {
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-1);
  }

  .testimonial__product {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin: 0;
  }

  @media (max-width: 767px) {
    .testimonials__grid {
      gap: var(--spacing-4);
    }
  }
</style>

{% schema %}
{
  "name": "Testimonials",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "What Our Customers Say"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "textarea",
          "id": "text",
          "label": "Review Text",
          "default": "Amazing product! Exactly what I was looking for. Great quality and fast shipping."
        },
        {
          "type": "text",
          "id": "author",
          "label": "Customer Name",
          "default": "John D."
        },
        {
          "type": "text",
          "id": "product",
          "label": "Product Name (Optional)",
          "default": "Wireless Headphones"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "text": "Amazing product! The quality exceeded my expectations and shipping was super fast.",
            "author": "Sarah M.",
            "product": "Wireless Earbuds"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "text": "Best purchase I've made this year. Customer service was incredibly helpful too!",
            "author": "Michael R.",
            "product": "Phone Case"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "text": "Love it! Perfect fit and the material feels premium. Will definitely order again.",
            "author": "Emily T.",
            "product": "Laptop Stand"
          }
        }
      ]
    }
  ]
}
{% endschema %}
