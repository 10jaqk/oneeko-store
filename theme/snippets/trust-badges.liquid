<div class="trust-badges">
  <div class="trust-badge">
    <span class="trust-badge__icon">
      {%- render 'icon-truck' -%}
    </span>
    <span class="trust-badge__text">{{ 'products.product.shipping.free_shipping_over' | t: amount: '$50' }}</span>
  </div>

  <div class="trust-badge">
    <span class="trust-badge__icon">
      {%- render 'icon-return' -%}
    </span>
    <span class="trust-badge__text">{{ 'products.product.trust.easy_returns' | t }}</span>
  </div>

  <div class="trust-badge">
    <span class="trust-badge__icon">
      {%- render 'icon-shield' -%}
    </span>
    <span class="trust-badge__text">{{ 'products.product.trust.secure_checkout' | t }}</span>
  </div>
</div>

<style>
  .trust-badges {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-3);
    padding: var(--spacing-4);
    background-color: var(--color-bg-secondary);
    border-radius: var(--radius-md);
    margin-bottom: var(--spacing-6);
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: var(--spacing-3);
  }

  .trust-badge__icon {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    color: var(--color-brand-primary);
  }

  .trust-badge__text {
    font-size: var(--font-size-sm);
    color: var(--color-text-primary);
    font-weight: var(--font-weight-medium);
  }
</style>
